# Core — the pipeline will not import without these.
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
torch>=2.0
h5py>=3.8          # NewData loading (common/load_2025.py)
PyWavelets>=1.4    # wavelet_packet transform, used by final_model.build()

# Optional transforms. Only needed for the corresponding --tfd-method.
EMD-signal>=1.5    # hht, emd
vmdpy>=0.2         # vmd
stockwell>=1.2     # S-transform
